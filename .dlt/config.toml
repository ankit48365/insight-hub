# This file configures the general behavior of dlt

[runtime]
# Instruct dlt to use Google Secret Manager as the default secrets provider.
# The project_id below must have the Secret Manager API enabled.
secrets_provider = "google_secrets_manager"
project_id = "your-gcp-project-id"  # TODO: Set your GCP Project ID here